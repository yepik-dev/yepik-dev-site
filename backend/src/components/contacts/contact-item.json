{
  "collectionName": "components_contacts_contact_items",
  "info": {
    "displayName": "contactItem"
  },
  "options": {},
  "attributes": {
    "label": {
      "type": "enumeration",
      "enum": [
        "tel",
        "email",
        "linkedin",
        "github",
        "facebook",
        "twitter",
        "instagram",
        "telegram"
      ]
    },
    "link": {
      "type": "string",
      "required": true
    },
    "contactType": {
      "type": "enumeration",
      "default": "link",
      "enum": [
        "tel",
        "email",
        "link"
      ]
    }
  },
  "config": {}
}
