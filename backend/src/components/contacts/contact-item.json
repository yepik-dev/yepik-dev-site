{
  "collectionName": "components_contacts_contact_items",
  "info": {
    "displayName": "contactItem"
  },
  "options": {},
  "attributes": {
    "label": {
      "type": "string",
      "required": true
    },
    "link": {
      "type": "string",
      "required": true
    },
    "contactType": {
      "type": "enumeration",
      "required": false,
      "default": "link",
      "enum": [
        "tel",
        "mail",
        "link"
      ]
    }
  },
  "config": {}
}
